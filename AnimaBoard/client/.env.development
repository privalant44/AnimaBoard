# En dev, le client (3001) appelle le serveur (3000) via cette URL.
# Si le proxy du serveur de dev échoue ("connexion refusée"), cette variable force les appels directs.
REACT_APP_API_URL=http://localhost:3000
